import React from 'react'
import { router } from 'base'
import './styles.styl'

export default class Title extends React.Component {
  constructor(props) {
    super(props)
  }

  render() {
    return (
        <div ref="component" className={`component title`}>
          BIsfGssssdffsdf
        </div>
    )
  }
}
